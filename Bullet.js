class Bullet {
  constructor(startPos, velocity, speed, size, colour) {
    this.bulletPos = startPos;
    this.velocity = velocity;
    this.speed = speed;
    this.size = size;
    this.colour = colour;
    this.maxSpeed = 5;
  }
}
